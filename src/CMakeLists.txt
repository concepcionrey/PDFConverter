
ADD_EXECUTABLE(PdfParser PdfParser.cpp)

TARGET_LINK_LIBRARIES(PdfParser ${PODOFO_LIB})
SET_TARGET_PROPERTIES(PdfParser PROPERTIES COMPILE_FLAGS "${PODOFO_CFLAGS}")
ADD_DEPENDENCIES(PdfParser ${PODOFO_DEPEND_TARGET})
INCLUDE_DIRECTORIES(${PoDoFo_SOURCE_DIR})
